import { TruncamientoPipe } from './truncamiento.pipe';

describe('TruncamientoPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncamientoPipe();
    expect(pipe).toBeTruthy();
  });
});
